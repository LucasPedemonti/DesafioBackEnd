<div id="product-details" class="contenedor-principal">
  <p class="prod">Detalle del producto</p>
  <div class="card-container">    
    <div class="card-detail">
      <img class="prod-img" src="{{thumbnail}}" alt="">
      <h1 class="card-title">{{name}}</h1>
      <p class="card-text">{{description}}</p>
      <p class="card-text">Categoría: {{category}}</p>
      <p class="card-text">Stock: {{stock}}</p>
      <p class="card-text">Precio: ${{price}}</p>
      <button type="button" class="button-scd button-add-to-cart" id="{{_id}}">Agregar al Carrito</button>
    </div> 
  </div>
</div>




<script src="/js/product.js"></script>